package dev.iconpln.mims.data.local.database;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TPHOTO".
*/
public class TPhotoDao extends AbstractDao<TPhoto, Long> {

    public static final String TABLENAME = "TPHOTO";

    /**
     * Properties of entity TPhoto.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PhotoNumber = new Property(1, Integer.class, "PhotoNumber", false, "PHOTO_NUMBER");
        public final static Property NoDo = new Property(2, String.class, "NoDo", false, "NO_DO");
        public final static Property Path = new Property(3, String.class, "Path", false, "PATH");
        public final static Property Type = new Property(4, String.class, "Type", false, "TYPE");
    }


    public TPhotoDao(DaoConfig config) {
        super(config);
    }
    
    public TPhotoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TPHOTO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"PHOTO_NUMBER\" INTEGER," + // 1: PhotoNumber
                "\"NO_DO\" TEXT," + // 2: NoDo
                "\"PATH\" TEXT," + // 3: Path
                "\"TYPE\" TEXT);"); // 4: Type
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TPHOTO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TPhoto entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer PhotoNumber = entity.getPhotoNumber();
        if (PhotoNumber != null) {
            stmt.bindLong(2, PhotoNumber);
        }
 
        String NoDo = entity.getNoDo();
        if (NoDo != null) {
            stmt.bindString(3, NoDo);
        }
 
        String Path = entity.getPath();
        if (Path != null) {
            stmt.bindString(4, Path);
        }
 
        String Type = entity.getType();
        if (Type != null) {
            stmt.bindString(5, Type);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TPhoto entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer PhotoNumber = entity.getPhotoNumber();
        if (PhotoNumber != null) {
            stmt.bindLong(2, PhotoNumber);
        }
 
        String NoDo = entity.getNoDo();
        if (NoDo != null) {
            stmt.bindString(3, NoDo);
        }
 
        String Path = entity.getPath();
        if (Path != null) {
            stmt.bindString(4, Path);
        }
 
        String Type = entity.getType();
        if (Type != null) {
            stmt.bindString(5, Type);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public TPhoto readEntity(Cursor cursor, int offset) {
        TPhoto entity = new TPhoto( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // PhotoNumber
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // NoDo
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // Path
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // Type
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TPhoto entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPhotoNumber(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setNoDo(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setPath(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setType(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(TPhoto entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(TPhoto entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(TPhoto entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
